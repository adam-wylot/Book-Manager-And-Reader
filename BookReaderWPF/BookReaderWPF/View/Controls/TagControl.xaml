<UserControl x:Class="BookReader.View.TagControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding Tags, ElementName=Root}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="{Binding ChipBackground, ElementName=Root}" 
                        CornerRadius="12" Margin="0,0,5,5" Padding="8,3">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" 
                                   Foreground="{Binding ChipForeground, ElementName=Root}" 
                                   Margin="0,0,5,0" FontSize="11" VerticalAlignment="Center"/>

                        <Button Content="×" 
                                Command="{Binding RemoveCommand, ElementName=Root}" 
                                CommandParameter="{Binding}"
                                Visibility="{Binding IsEditable, ElementName=Root, Converter={StaticResource BoolToVis}}"
                                Background="Transparent" BorderThickness="0" Cursor="Hand" 
                                Foreground="LightGray" Padding="0,-2,0,0"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>