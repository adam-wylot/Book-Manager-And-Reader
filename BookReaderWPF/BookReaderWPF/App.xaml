<Application x:Class="BookReader.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BookReader"
             StartupUri="View/Windows/MainWindow.xaml">
    
    <Application.Resources>
        <ResourceDictionary>
            <views:Base64ToImageConverter xmlns:views="clr-namespace:BookReader.View" x:Key="Base64ToImageConverter" />
            
            <SolidColorBrush x:Key="PrimaryColorBrush" Color="#568c90" />
            <SolidColorBrush x:Key="DarkerColorBrush" Color="#4d7980" />
            <SolidColorBrush x:Key="LighterColorBrush" Color="CadetBlue" />
            <SolidColorBrush x:Key="AccentColorBrush" Color="Orange" />
            <SolidColorBrush x:Key="AccentDarkerColorBrush" Color="#B2998A" />

            <!-- Płaskie przyciski-->
            <Style TargetType="Button" x:Key="FlatButton">
                <Setter Property="Background" Value="{StaticResource PrimaryColorBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="15,8"/>
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="BgBorder" Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="BgBorder" Property="Background" Value="#D3D3D3"/>
                                    <Setter Property="Foreground" Value="#999999"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="1.0"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
